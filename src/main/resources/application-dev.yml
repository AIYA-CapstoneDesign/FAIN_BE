spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fain_db
    username: root
    password: ${DATA_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect



  jwt:
    secret: ${JWT_SECRET}